[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "minions-test-reports"
version = "0.2.0"
description = "Aggregated results, regression diffs, quality trends, and coverage maps"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.11"
keywords = ["test-reports","ai","minions"]
authors = [
    { name = "Mehdi Nabhani", email = "mehdi@the-mehdi.com" }
]
dependencies = [
    "minions-sdk>=0.2.1",
]

[project.optional-dependencies]
test = ["pytest>=7.0"]

[project.urls]
Homepage = "https://github.com/mxn2020/minions-test-reports"
Repository = "https://github.com/mxn2020/minions-test-reports"

[tool.hatch.build.targets.wheel]
packages = ["minions_test_reports"]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
